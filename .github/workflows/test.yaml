name: Deploy

on: [pull_request]

concurrency: '1'

jobs:
  build:
    runs-on: Ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      - run: |
          echo $GITHUB_ACTOR
          git config user.name $GITHUB_ACTOR
          git config user.email $GITHUB_ACTOR@users.noreply.github.com
          git fetch origin storybook-static || true
          TEMP_DIR=$(mktemp -d)
          git worktree add --detach $TEMP_DIR storybook-static
          printf "Hello $GITHUB_REPOSITORY - PR #$(jq --raw-output .pull_request.number "$GITHUB_EVENT_PATH")" > $TEMP_DIR/index.html
          cd $TEMP_DIR
          # git add -A
          # git commit -am "Release ${{ env.GITHUB_SHA }}" || true
          # git push
          # if ! git fetch origin storybook-static; then
          #   echo "Create storybook-static branch"
          #   git checkout --orphan storybook-static
          #   git rm --cached -r .
          #   git add -A .
          #   git push
          #   git checkout
          # fi
          # echo "Done"
